'use client';
import {useState} from 'react';
export default function Advisor(){const [grade,setGrade]=useState('D+');const [items,setItems]=useState<any[]>([]);async function run(){const res=await fetch(`${process.env.NEXT_PUBLIC_API_URL}/api/v1/ai/course-recommendations`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({kcseGrade:grade,interests:['ICT','Hospitality'],budgetPerTerm:25000})});setItems((await res.json()).recommendations||[])}return <main style={{padding:32,fontFamily:'sans-serif'}}><h1>Find My Best ICS Course in 60 Seconds</h1><label>KCSE Grade <input value={grade} onChange={e=>setGrade(e.target.value)} /></label><button onClick={run}>Recommend Courses</button>{items.map(x=><div key={x.courseOfferingId} style={{border:'1px solid #ddd',marginTop:12,padding:12,borderRadius:12}}><b>{x.course}</b><p>{x.level} • {x.examBody} • Match {x.matchScore}%</p><p>Tuition: Ksh {x.tuitionFeePerTerm} | Practical: Ksh {x.practicalFeePerTerm}</p></div>)}</main>}

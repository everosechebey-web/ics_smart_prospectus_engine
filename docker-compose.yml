services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: ics
      POSTGRES_PASSWORD: ics
      POSTGRES_DB: ics_prospectus
    ports: ["5432:5432"]
    volumes: ["pgdata:/var/lib/postgresql/data"]
volumes: { pgdata: {} }
